###############################################
# Backend .env Example
# 用途：复制为 .env 或 .env.local，然后按需填写。
# 生产环境请通过 CI/参数存储（SSM/Secrets Manager）注入，不要提交真实密钥。
#
# 快速开始（本地）
# 1) 部署基础设施：npm run cdk:deploy:dev
#    部署完成后控制台会输出 EnvFileContent（推荐直接复制到 backend/.env）。
# 2) 或手动参考下方注释填入值。
# 3) 启动：npm run start:dev
###############################################

# Application Configuration
NODE_ENV=development                  # development | production | test
PORT=3000
APP_NAME=linuo-aws-template-backend

# API Configuration
API_PREFIX=api/v1
SWAGGER_TITLE=linuo-aws-template API
SWAGGER_DESCRIPTION=linuo-aws-template API for Trade Management System
SWAGGER_VERSION=1.0.0
# SWAGGER_ENABLED=                     # 可选：仅 Lambda 环境变量示例使用；应用实际根据 NODE_ENV 控制

# AWS Configuration
AWS_REGION=ap-southeast-1            # 与 CDK 部署区域保持一致
# 本地凭证建议通过 AWS CLI 配置（aws configure），或使用默认 profile
# 可选（不建议写入文件）：
# AWS_PROFILE=default
# AWS_ACCESS_KEY_ID=xxx
# AWS_SECRET_ACCESS_KEY=xxx

# AWS S3 Configuration（导入/导出临时文件桶）
# 由 CDK 默认创建：<project>-import-export-<env>-<region>
S3_IMPORT_EXPORT_BUCKET_NAME=linuo-aws-template-import-export-dev-ap-southeast-1
S3_REGION=ap-southeast-1

# AWS Cognito Configuration
COGNITO_USER_POOL_ID=replace-with-user-pool-id
COGNITO_CLIENT_ID=replace-with-app-client-id
# COGNITO_CLIENT_SECRET=              # 如 App Client 生成了密钥则填写；模板默认未生成
COGNITO_REGION=ap-southeast-1
COGNITO_EMAIL_ALIAS_ENABLED=true      # 若 User Pool 配置邮箱为 alias/登录名，保持 true

# AWS DynamoDB Configuration
DYNAMODB_REGION=ap-southeast-1
DYNAMODB_TABLE_PREFIX=linuo-aws-template-dev   # 代码会与下方表名组合成 <prefix>-<table>

# Database Tables（当前仅使用产品表）
DB_TABLE_PRODUCTS=linuo-aws-template-dev-products
# 历史 users/customers 表已移除；无需配置 DB_TABLE_USERS/DB_TABLE_CUSTOMERS

# JWT Configuration
JWT_SECRET=dev-secret-key             # 本地开发用，生产环境请使用强随机字符串
JWT_EXPIRES_IN=24h

# Deployment Information（参考用，可留空）
API_GATEWAY_URL=https://xxxxxx.execute-api.ap-southeast-1.amazonaws.com/dev/
API_GATEWAY_ID=xxxxxx
API_LAMBDA_FUNCTION_NAME=linuo-aws-template-api-dev

# 变量来源提示：
# - 部署后，CDK 会在输出中提供 "EnvFileContent"，可直接复制为 .env
# - 或在 AWS 控制台查看 S3/Cognito/DynamoDB 资源并填入对应值
